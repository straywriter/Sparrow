//#include "Core/Config.h"

#include "Math/Detail/Matrixf.h"
#include "Math/Detail/TVector.h"
// #include <cassert>
#include <assert.h>

using namespace std;
using namespace Math;

//------------tvector----------------
/*
template<typename T, size_t size>
inline Math::TVector<T, size>::TVector()
{
	for (auto i = size; i--; this->data[i] = static_cast<T>(0))
		;
}

template<typename T, size_t size>
inline Math::TVector<T, size>::TVector(T val)
{
	for (auto i = size; i--; this->data[i] = val)
		;
}
*/

//---------------tvector2----------






//---------------tvector4-----------




